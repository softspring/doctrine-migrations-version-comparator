{
    "name": "softspring/doctrine-migrations-version-comparator",
    "description": "Provides a class to sort doctrine migrations by version name",
    "type": "library",
    "license": "MIT",
    "minimum-stability": "dev",
    "prefer-stable": true,
    "require": {
        "php": ">=8.1",
        "doctrine/doctrine-migrations-bundle": "^3.0",
        "doctrine/orm": "^2.6 || ^3"
    },
    "require-dev": {
        "friendsofphp/php-cs-fixer": "3.58.* || 3.60.* || 3.64.*",
        "phpstan/phpstan": "^1.9",
        "rector/rector": "^1.0"
    },
    "autoload": {
        "psr-4": {
            "Softspring\\Component\\DoctrineMigrationsVersionComparator\\": "src/"
        }
    },
    "config": {
        "sort-packages": true
    }
}
